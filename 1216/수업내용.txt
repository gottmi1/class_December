필터 라이트룸에 있는 고급 색보정기능을 사용할 수 있다.

컨트롤 시프트 a

소스편집시 -> 색조를 계열로 -> 어우러지게 연출가능

AF 오토 포커스


vector 

svg -> 직접 코드를 가져오는 것이 훨씬 경제적이다.
svg 확장자 자체가 xml -> element로 활용할 수 있다.


일러스트레이터

stroke - 면으로 만드는거 -> 메뉴 -> path -> outlinestroke
스트로크 컨 f10 (option에 뭐잇음)
pathfinder 컨 시 f9


svg -> layer에서 먼가 편집 -> layer로 뜯고싶다
layer(f7) 옵션 -> rellase to layer(sequence) <- 패스,클리핑마스크,그룹등이 다 layer로 바뀜

ai - effect
쉐입을 변형하고 싶을 때
effect menu

변형 -> 쉐입 변형 -> 확정
오브젝트 메뉴-> 인스팬드 어페어런스 <-- 사용할 수 있음


메인이 글씨여야 할 때
typography

문자 : 암호 -> 규칙성을 해체 -> 관심을 끌게 함 -> 디자인 강조로 변함

가독성 : 잘 읽히는 것
가해성 : 어디까지 해설할 수 있느냐

가독성이 매번 좋을 필요는 업다 (필요에 따라 구려도 됨)

글씨로만 구성된 레이아웃의 가능성 타이포그래피
글씨로만 구성하면 오히려 심플+화려함 둘 다 연출 가능.

git branch
현재 브랜치 확인
git branch 이름
만들기
git checkout 바꿀 이름
현재 branch가 바뀜