포폴 -> 
내가 계속 업데이트 하며 관리할 나의 홈페이지라고 생각하고 만드는 방법도 있음

git

개발환경 세팅

1. IDE(이클립스, VS, 파이선 등의 프로그램)
2. git -> 로컬시스템에 git 설치(git bash터미널이 생김)
3. cli를 연습하고자 하면 -> linux명령어를 공부

- 왜 굳이 마우스를 안쓰냐?
1) 익숙해지면 개빠름
2) 소통해야할 대상인 백엔드 개발자들이 이걸 사용하기 때문(커뮤니케이션 이슈)
-> why? 우리가 알고있는 server는 window를 깔지 않고 linux를 사용함


-git branch 
브랜치가 몇 개 있는지 나옴
- git branch -m 이름
해당 브런치의 이름을 새것으로 변경 

git에 올릴떈 한 폴더에 폴더를 존나 만들지 말고
브랜치를 새로 만들어서 거기다가 하나씩 올리면 개꿀

작업할 때 : 브랜치 생성
브랜치로 들어가서 새로운 작업을 진행
-> 회사에선 이렇게 보여줌 -> commit을 통해 마스터 관리자에게 결재(작업물에 대한 컨펌)받음
작업 관리자 : 여러 브랜치를 합치는 과정을 수행함

메인 브랜치에서 git merge `새로만든 브랜치`


주요

git init -> 이니셜라이즈 (필수
git add -> 스테이징 .
git commit -m -> 커밋쓰기
git clone -> 해당 주소의 레퍼지토리 복사해오기
git push -> 해당 주소에 밀어넣기
git pull -> 해당 주소거 떙겨오기(똑같은 폴더에 업데이트 된 내용이 있을경우 사용함)
git branch 뒤에 폴더 이름 쓰면 만들고 안쓰면 현재 브랜치 조회하고 -d 쓰면 지우고
git checkout -> 브랜치 변경

https://backlog.com/git-tutorial/kr/stepup/stepup2_4.html
여기 참고하면 좋을듯